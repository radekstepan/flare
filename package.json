{
  "name": "root",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "release": "lerna publish --registry=https://npm.pkg.github.com/"
  }
}
