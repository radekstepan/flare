{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "release": "lerna publish --registry=http://localhost:4873/"
  }
}
